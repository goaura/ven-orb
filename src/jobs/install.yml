description: >
  Installs 'ven' and dependencies.

executor: default

parameters:
  fingerprint:
    type: string
    description: "CircleCI SSH Fingerprint"
steps:
  - checkout
  - add_ssh_keys:
      fingerprints:
        - << parameters.fingerprint >>
  - install
