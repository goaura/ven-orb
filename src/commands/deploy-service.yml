description: >
  Deploys Infrastructure

steps:
  - run:
      name: ven --stage prod deploy service --extra-tag ${CIRCLE_SHA1}
      command: ven --stage prod deploy service --extra-tag ${CIRCLE_SHA1}
